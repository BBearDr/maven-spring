package com.bbear.example.lintCode;

/**
 * 反转单链表
 * 1->2->3->null ,反转后 null<-1<-2-<3
 *
 * @author junxiongchen
 * @date 2018/03/07
 */
public class ReserveNode {
    /*ActList* reserveList(ActList* head){
        if (Null == head || Null == head->next){xingfu
            return head;
        }
        ActList* p,q,r;
        p = head;
        q = head->next;
        head-next = null;
        while(q){
            r = q->next;
            q->next = p;
            p = q;
            q = r;
        }
        //最后q指向null，所以p作为新的指针头
        head = p;
        return head;
    }*/
}
